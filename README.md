# Cask Engine

### Installation
```shell
# after cloning the repo, from the root directory
git clone git@github.com:s-ajensen/cask-lib.git
mv cask-lib lib
```

### Run a sample
```shell
clj -Mtriangle
```